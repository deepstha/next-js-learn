'use client';
import React from 'react'

const AddToCart = () => {
  return (
    <div>
       <button className="bg-blue-500 btn btn-primary" onClick={() => console.log('clicked Add to cart')}>Add to Cart</button>
    </div>
  )
}

export default AddToCart
